import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const BaseImages: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">
      Base Images Page Title
    </Title>
  </PageSection>
);

export { BaseImages };
